# The following code contains errors.
# You need to do 5 corrections on the code.
# Try to do corrections on online-python.com OR IDLE PYTHON before you commit the changes.

def calculation(a, b):
    subtraction = a * b
    multiplication = a + b
    division = a - b
    return a, b, division  # return 3 values separated by comma

result = calculations(30, 5) # Call the function to get the result. Use 30 and 5 as argument.
print(result) # Don't change the code. It shows the output as "(25, 150, 6.0)"
